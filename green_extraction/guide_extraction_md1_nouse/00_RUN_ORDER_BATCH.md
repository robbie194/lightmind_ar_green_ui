# Codex 执行顺序说明（批量自动提取绿色部分版）

## 用户目标（请严格按此理解）
从目录 **`/home/robbie/tyf_file/startup/lightMind/code_ui_ar/leqi_ui`** 中的所有参考图里，**批量自动检测并提取绿色 UI / 图标部分**，去除背景，输出：
- 透明背景 PNG（候选素材）
- 可选：矢量化 SVG（候选素材）
- 一个本地预览页（gallery）用于快速查看提取结果（不是逐张看原图）

> 这些参考图中的绿色图标/绿色UI部分都是我想要的。请优先自动化处理，不要求我逐张手工标注。

---

## 必须遵守的原则
1. **先做批量提取工具，再做预览筛选，再做矢量化**
2. **不要要求用户逐张手工框选或标注**
3. **优先保证透明背景输出正确**
4. **先产出批量结果，再调参数提升质量**
5. **尽量自动化，但允许提供阈值参数以便二次重跑**

---

## 执行阶段（必须按顺序）

### 阶段 1：实现批量绿色提取工具（Python）
请先执行 `01_BATCH_GREEN_EXTRACTION_TASK.md`，输出一个可运行的 Python 工具项目，完成：
- 扫描输入目录所有图片
- 自动检测绿色区域
- 分割为多个候选图块
- 导出透明 PNG
- 生成中间掩膜（可选）
- 输出统计报告（总图数、候选数、过滤数）

> 阶段 1 重点是提取质量与可批量运行，不需要精美UI。

---

### 阶段 2：实现本地预览页（Gallery）
执行 `03_GALLERY_REVIEW_UI_TASK.md`，输出一个静态 HTML 页面，用于快速查看提取结果：
- 展示所有候选透明 PNG / SVG
- 显示来源文件名和编号
- 支持按来源/尺寸/面积筛选（至少一项）
- 支持快速删除/隐藏低质量候选（可选）

> 目的是减少人工工作量，不是逐张看原图。

---

### 阶段 3：实现矢量化（可选但建议）
执行 `02_VECTORIZATION_TASK.md`，将提取到的候选（PNG/mask）批量矢量化为 SVG：
- 输出轮廓路径
- 提供简化参数
- 保留透明背景（SVG默认透明）
- 可在SVG中重建绿色描边/发光（简化版）

---

### 阶段 4：自检与交付
执行 `04_ACCEPTANCE_CHECKLIST_BATCH.md` 自检，并给出：
- 已满足项
- 未满足项（及原因）
- 下一步建议（如阈值调优）

---

## 禁止事项
- 不要要求用户手工逐张圈选绿色区域
- 不要只做“单图脚本”
- 不要把整张原图直接导出当作结果
- 不要忽略透明背景要求
- 不要省略可配置阈值（否则难以调优）

---

## 输出要求（最终）
请按文件分段输出完整项目代码，至少包括：
- 批量提取脚本（Python）
- 配置文件（阈值/参数）
- 本地预览页（HTML/CSS/JS）
- 矢量化脚本（Python，可选但建议）
- README（运行方法、参数说明、目录结构）

并在 README 中默认写死输入目录为：
`/home/robbie/tyf_file/startup/lightMind/code_ui_ar/leqi_ui`
同时支持命令行参数覆盖。
