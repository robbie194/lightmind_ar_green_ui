1. - 

   - ---
   
     ## `04_ACCEPTANCE_CHECKLIST.md`
   
     ```md
     # 验收清单（给 Codex 自检 + 给用户验收）
     
     ## A. 流程正确性（阶段性）
     - [ ] 已先输出 `STYLE_SPEC.md` 和 `COMPONENT_PLAN.md`
     - [ ] 在用户确认前未进入编码阶段
     - [ ] 编码阶段严格遵循风格规范
     
     ---
     
     ## B. 页面基础可用性
     - [ ] 打开 `index.html` 后页面可直接使用
     - [ ] 无需后端
     - [ ] 无需构建步骤（或已在 README 清晰说明）
     - [ ] 页面能看到组件预览区和控制区
     
     ---
     
     ## C. 风格一致性（重点）
     - [ ] 整体为绿色 HUD / AR 叠加风格（非霓虹灯牌风）
     - [ ] 图标为统一线框风格（stroke-only）
     - [ ] 线宽统一（可通过控制区调整）
     - [ ] 圆角统一（可通过控制区调整）
     - [ ] 发光轻微且清晰（可通过控制区调整）
     - [ ] 图标卡片、信息面板、提示条风格一致
     
     ---
     
     ## D. 组件数量与类型
     - [ ] 图标卡片类 >= 4 个
     - [ ] 信息面板类 >= 1 个
     - [ ] 横向提示条类 >= 1 个
     - [ ] 状态栏元素 >= 1 组
     
     ---
     
     ## E. 控制区功能（必须可用）
     - [ ] 主色可调并实时生效
     - [ ] 线宽可调并实时生效
     - [ ] 发光强度可调并实时生效
     - [ ] 圆角可调并实时生效
     - [ ] （可选）缩放可调
     
     ---
     
     ## F. SVG 导出（每个组件）
     - [ ] 每个组件都有“导出 SVG”按钮
     - [ ] 导出内容为目标组件本体，不含页面背景/其他组件
     - [ ] 导出背景透明
     - [ ] 导出文件可正常打开（主流浏览器）
     - [ ] 文件名有意义
     
     ---
     
     ## G. PNG 导出（每个组件）
     - [ ] 每个组件都有“导出 PNG”按钮
     - [ ] 导出背景透明
     - [ ] 默认至少 2x 清晰度
     - [ ] 不包含页面背景/按钮UI
     - [ ] PNG 导出在常见浏览器可用
     
     ---
     
     ## H. 导出实现可靠性（关键技术点）
     - [ ] SVG 导出使用序列化而非页面截图
     - [ ] PNG 导出基于 SVG -> Canvas 转换
     - [ ] Canvas 未填充背景色
     - [ ] 使用了正确的 SVG 尺寸 / viewBox
     - [ ] glow/filter 不被裁切（filter 范围已扩大）
     - [ ] 导出 SVG 内包含必要的 defs/filter（如果使用 glow）
     
     ---
     
     ## I. 代码质量
     - [ ] 文件结构清晰
     - [ ] 关键函数有注释（尤其导出函数）
     - [ ] 无明显重复冗余代码
     - [ ] 命名清晰
     - [ ] README 完整可用
     
     ---
     
     ## J. README 完整性
     - [ ] 运行方式说明
     - [ ] 控制区使用说明
     - [ ] 导出 SVG/PNG 说明
     - [ ] 已知限制说明
     - [ ] 扩展新组件说明（简短）
     
     ---
     
     ## K. 禁止项检查（必须通过）
     - [ ] 未复制参考图具体图标路径
     - [ ] 未完整复刻参考图布局
     - [ ] 未做成重霓虹/厚重面板风格
     - [ ] 未省略真实导出逻辑（不是假按钮）
     
     ---
     
     ## 验收结论模板（给 Codex）
     请在最终输出末尾附上以下结构（示例）：
     
     ### 自检结果
     - 已满足：A/B/C/...（列出）
     - 可选未实现：xxx（如果有）
     - 已知限制：xxx（如果有）
     - 后续建议：xxx（可选）
