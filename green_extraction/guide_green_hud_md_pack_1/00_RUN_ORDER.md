1. # Codex 执行顺序说明（必须遵守）

   ## 目标
   基于我提供的多张参考图，先提炼统一的绿色 HUD / AR 风格规范，再按规范实现一个可直接使用的静态 HTML UI 页面，支持导出透明背景的 SVG / PNG。

   ---

   ## 重要原则（必须）
   1. **先做风格学习与规范提炼，再写代码**
   2. **只学习风格语法，不复制原图内容**
   3. **优先保证导出 SVG/PNG 功能可靠**
   4. **先做最小可用版本，再扩展组件数量**

   ---

   ## 执行阶段（按顺序执行，不能跳过）

   ### 阶段 1：风格学习（不写代码）
   请先阅读：
   - `01_STYLE_LEARNING_TASK.md`
   - `02_REFERENCES_INDEX_TEMPLATE.md`（我会填写参考图用途）
   - 所有参考图文件

   输出：
   - `STYLE_SPEC.md`（视觉风格规范）
   - `COMPONENT_PLAN.md`（组件实现计划）

   > 阶段 1 不要输出 HTML/CSS/JS 代码。

   ---

   ### 阶段 2：等待我确认
   在我确认 `STYLE_SPEC.md` 和 `COMPONENT_PLAN.md` 前，不要进入编码阶段。

   ---

   ### 阶段 3：实现 UI 项目（开始写代码）
   在我确认后，再阅读并执行：
   - `03_UI_IMPLEMENTATION_TASK.md`
   - `04_ACCEPTANCE_CHECKLIST.md`

   输出完整项目文件，至少包括：
   - `index.html`
   - `styles.css`
   - `script.js`
   - `README.md`

   ---

   ### 阶段 4：自检与修正
   请根据 `04_ACCEPTANCE_CHECKLIST.md` 逐项自检，并在输出中说明：
   - 哪些已满足
   - 哪些是可选项未实现（如有）
   - 已知限制（如字体一致性）

   ---

   ## 禁止事项（非常重要）
   - 不要直接复刻参考图中的完整界面布局
   - 不要复制参考图中的具体图标路径（SVG path）
   - 不要做成霓虹灯牌风格（过重发光、粗描边）
   - 不要省略导出 SVG / PNG 按钮后假装实现
   - 不要先写代码再反推风格规范

   ---

   ## 输出风格要求
   - 中文说明
   - 结构清晰
   - 文件内容按文件名分段输出
   - 对关键函数（尤其导出逻辑）添加注释
