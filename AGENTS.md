# Repository Agent Rules

## Git workflow (mandatory)

- After every code or document change, run `git status`.
- Commit the intended modified files with a clear commit message.
- Push to remote repository: `https://github.com/robbie194/lightmind_ar_green_ui`.
- This is the default behavior for all future modifications unless the user explicitly says otherwise.
